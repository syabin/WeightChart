# 2026-08-11

- 完成重量波动折线图网页初版：离线 HTML + ECharts + SheetJS，支持 Excel 导入、列映射、波动阈值、框选/滚轮/滑块放大、悬停 tooltip、统计面板、PNG/CSV 导出。
- 根据用户真实数据（胶粉计量罐.xlsx，43k+ 行，2 秒采样，重量量化到 0.01kg）诊断并修复：
  - 折线不显示：ECharts 数据格式由 `{t,w}` 对象改为 `[timestamp, weight]` 数组。
  - 0 事件：读取改为 `raw:true` 保留原始精度，事件检测由死区改为「10 秒窗口内最大-最小波动」并聚合事件段统计。
